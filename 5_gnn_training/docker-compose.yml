services:
  # neo4j_db:
  #   image: neo4j:5.26-bullseye
  #   container_name: neo4j_read_container
  #   ports:
  #     - "7475:7474"
  #     - "7688:7687"
  #   volumes:
  #     - "~/git/SepsisEvalPipeline/4_db_upload/neo4j_data:/data"
  #     - "~/git/SepsisEvalPipeline/3_graph_construction/data:/var/lib/neo4j/import"
  #   environment:
  #     NEO4J_AUTH: neo4j/password
  #     NEO4J_server_memory_heap_initial__size: 2G
  #     NEO4J_server_memory_heap_max__size: 2G
  #     NEO4J_server_memory_pagecache_size: 1G
  #   healthcheck:
  #     test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:7474 || exit 1"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  gnn_model:
    build: .
    container_name: gnn_training_container
    # depends_on:
    #   neo4j_db:
    #     condition: service_healthy
    volumes:
      - ../config.ini:/app/config/config.ini
      - ../0_mimic_preprocess:/app/0_mimic_preprocess
      - ./checkpoints:/app/checkpoints
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]